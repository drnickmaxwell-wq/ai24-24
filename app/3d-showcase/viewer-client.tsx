'use client';

// Put your R3F / Canvas or 3D code here. This is just a safe placeholder.
export default function ViewerClient() {
  return (
    <div className="mt-6 rounded-xl bg-white/80 backdrop-blur-md border border-white/30 p-6">
      <p className="text-sm text-[var(--text)]/70">
        3D viewer loads client-side here (React-Three / AR, etc.).
      </p>
    </div>
  );
}
